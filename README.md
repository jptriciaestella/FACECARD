# face_card

#### you have to install cmake from https://cmake.org/download/ and choose the latest stable version that compatibles with your platform. after cmake installed, make sure you add cmake path (directory to cmake) to your system environtment variables

#### now, you can install the requirement using pip install -r requirements.txt